import '@storybook/addon-vitest/internal/setup-file';

// @storybook/addon-a11y preview annotations (afterEach axe checks) are applied
// automatically by @storybook/addon-vitest >= 10.3 via
// setup-file-with-project-annotations — no manual annotation wiring needed.
